# Example environment variables for docker-compose.db.yml
# Copy this file to docker-db.env and adjust values before running docker compose.

# PostgreSQL configuration
DB_NAME=zalopay
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST_PORT=5433

# pgAdmin configuration
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=change-me
PGADMIN_HOST_PORT=8081
